# Agent runtime — what `parkinson_agent/` actually needs.
ollama>=0.4.0
pydantic>=2.7
json-repair>=0.30   # auto-repair malformed JSON from small LLMs (simple mode)

# Upstream pipeline (`models/generate_knowledge.py`) — CSV → data.json.
numpy>=1.26
scipy>=1.11
pandas>=2.0

# Testing
pytest>=8.0

# Demo UI (only needed if you run the Streamlit clinician view)
streamlit>=1.32
